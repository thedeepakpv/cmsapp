import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6a1b9a"
    },
    secondary: {
      main: "#ff6f00"
    },
    background: {
      default: "#f3e5f5"
    }
  }
});

export default theme;
